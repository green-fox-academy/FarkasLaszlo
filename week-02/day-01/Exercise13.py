current_hours = 14;
current_minutes = 34;
current_seconds = 42;

# Write a program that prints the remaining seconds (as an integer) from a
# day if the current time is represented bt the variables
seconds = 24*60*60
current = 14*3600 + 34*60 + 42
print("Seconds left:", seconds-current )